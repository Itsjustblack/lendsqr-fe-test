# Test Environment Variables
# This file is used during test runs to ensure MSW intercepts all API calls

# Use localhost base URL for MSW interception
VITE_API_BASE_URL=http://localhost:3000/api

# Test API key (not used by MSW but prevents undefined in interceptor)
VITE_API_PRIVATE_KEY=test-key-123
